import { View, Text, StyleSheet, Button } from 'react-native'

export default function CreatePost() {
    // const router = useRouter();

    return (
        <View >
            <Text style={{color: 'white'}}>Temporary screen not yet needed</Text>
        </View>
    )
}