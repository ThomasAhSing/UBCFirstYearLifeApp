import {
  TouchableOpacity,
  StyleSheet,
  FlatList,
  Dimensions,
  Image,
  View,
} from 'react-native'


import { Colors } from '@/constants/Colors'

export default function RenderedConfession({ confession }) {
    <View style={styles.container}>

    </View>

}

const styles = StyleSheet.create({
  container: {
    width: '100%',
    height: '100%',
    backgroundColor: Colors.background
  },
})
